import React from "react";

export const SendButton = (props) => (
    <div> 
        <button className="btn outrunPink" {...props}>Send <i className="far fa-paper-plane"></i></button>

    </div>

);

export default SendButton;